"""
Divide two integers without using multiplication,
division, or mod operator.
"""

class Solution:
    def divide(dividend, divisor):
        assert isinstance(dividend, int)
        assert isinstance(divisor, int)
        if divisor == 0:
            raise Exception




#        binaryList = [divisor]
#        multipleOfDivisor = 1
#        totalDivisor = divisor
#        while dividend > totalDivisor + totalDivisor:
#            totalDivisor = totalDivisor + totalDivisor
#            multipleOfDivisor = multipleOfDivisor + multipleOfDivisor
#            binaryList.append(totalDivisor)
#        total = totalDivisor
#


